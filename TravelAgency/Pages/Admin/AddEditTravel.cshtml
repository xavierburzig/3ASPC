@page "{id?}"
@model AddEditTravelModel
@{
    ViewData["Title"] = Model.IsNewTravel ? "New Travel" : "Edit Travel";
}

@if (!ModelState.IsValid)
{
    <div class="col-md-offset-2 col-md-10">
        <h3 class="text-danger">Validation Errors</h3>
        <div asp-validation-summary="All"></div>
        <hr />
    </div>
}

<form method="post" class="form-horizontal" enctype="multipart/form-data">
    <div class="form-group">
        <label asp-for="Travel.Name" class="col-md-2 control-label"></label>
        <div class="col-md-10">
            <input asp-for="Travel.Name" class="form-control" />
            <span asp-validation-for="Travel.Name"></span>
        </div>
    </div>
    <div class="form-group">
        <label asp-for="Travel.Description" class="col-md-2 control-label"></label>
        <div class="col-md-10">
            <textarea rows="3" asp-for="Travel.Description" class="form-control"></textarea>
            <span asp-validation-for="Travel.Description"></span>
        </div>
    </div>
    <div class="form-group">
        <label asp-for="Travel.Activities" class="col-md-2 control-label"></label>
        <div class="col-md-10">
            <textarea rows="5" asp-for="Travel.Activities" class="form-control"></textarea>
            <span asp-validation-for="Travel.Activities"></span>
        </div>
    </div>
    <div class="form-group">
        <label asp-for="Image" class="col-md-2 control-label"></label>
        <div class="col-md-10">
            <input type="file" asp-for="Image" />
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <button type="submit" class="btn btn btn-primary">Save</button>
            @if (!Model.IsNewTravel)
            {
                <button type="submit" asp-page-handler="Delete" class="btn btn btn-warning">Delete</button>
            }
        </div>
    </div>
</form>